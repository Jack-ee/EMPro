# EMPro v120 — 语音链防停摆（含 v113—v119 全部内容，替代此前各包）

三处根治「播一半停止」与息屏失效：
- neural 语音 20 秒硬超时：移动网络假死时切设备音而不是永久挂起
  （手动停止仍不推进链条）。
- 包音频播放加时长看门狗：任何浏览器怪癖吞掉结束事件都会到点强制
  推进；blob URL 改为换源成功后再回收，消除事件被吞的竞态源头。
- Stories 连播补齐息屏策略：后台只播包内句子、缺失跳过，亮屏恢复
  完整链路（与 My Words 一致）。

操作：
1. 解压覆盖 → 提交推送；Worker 无改动（v119 粘贴过即可）。
2. 刷两次到 emp-v120。
3. 验收息屏前确认 MIUI 已给浏览器「无限制」后台并锁定任务，
   否则系统级杀进程无解。新故事的句子要等下一次音频构建
   （提交 wordlist 触发）才进包，在那之前后台播放会跳过它们、
   前台由 neural/设备音兜底——这是设计行为。
