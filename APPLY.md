# EMPro v122 — 息屏磁带模式（含 v113—v121 全部内容，替代此前各包）

此设备类别的后台连元素换源都拒绝，短剪辑链无论如何填缝都活不过
锁屏。v122 换打法：锁屏瞬间把当前卡到组尾的英文包音频拼接成
一个连续 blob，一次 play 放到底——后台零换源、零定时器，系统
眼中它就是一个播客。按字节偏移把播放进度映射回单词卡：锁屏
标题跟随当前词、上/下一词按偏移跳转、进度实时保存、亮屏无缝
接回交互链（含中文释义）。

操作：推送 → 刷两次到 emp-v122 → 开连播 → 息屏验收。
若这版仍在半分钟内断，且断时声音戛然而止、亮屏发现页面被
重载——那就是 MIUI 杀进程实锤，请再核对浏览器省电「无限制」
并在多任务里锁定；代码侧已到 Web 平台能力边界。
