# EMPro v124 — Stories 息屏磁带（含 v113—v123 全部内容，替代此前各包）

Stories 连播与 My Words 同款磁带化：锁屏瞬间把当前句到故事结尾
的包音频拼成一个连续 blob 一次播完——后台零换源零定时器；字节
偏移追踪当前句（行高亮跟随），亮屏无缝接回逐句链；锁屏卡显示
故事标题，可暂停。

操作：推送 → 刷两次到 emp-v124 → 打开故事点播放 → 息屏验收。
注意：故事句子必须已在音频包中（新生成的故事需先导出 wordlist
触发一次构建），否则磁带无料可拼、后台静默，属预期。
