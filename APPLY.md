# EMPro v121 — 息屏连播根治：后台无静默窗口
#（含 v113—v120 全部内容，替代此前各包）

病根：词间/卡间的 350/1200 毫秒间隙靠定时器实现，间隙中页面
完全无声——锁屏落在间隙里，系统即收回音频焦点，下一次 play()
被拒，连播就此中断。

修复：后台状态下所有间隙改为经同一常驻元素播放程序生成的
静音 WAV，媒体会话全程连续，系统无从收回焦点；前台仍用
定时器（行为不变）。覆盖 My Words 词间/卡间与 Stories 句间。

操作：
1. 解压覆盖 → 提交推送；Worker 无改动。
2. 刷两次到 emp-v121。
3. 验收：开自动连播 → 息屏 → 应持续到整组播完；锁屏卡
   上一词/暂停/下一词可用。仍需 MIUI「无限制」后台前提。
