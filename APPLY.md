# EMPro v119 — Drive 列表下钻一层（含 v113—v118 全部内容，替代此前各包）

- 按文献建子文件夹的归档方式直接支持：Worker 一次合并查询取出
  最多 25 个子文件夹内的音频，条目上显示所属文献名；顶层直接
  放文件同样有效；更深层级不下钻。

操作：
1. 解压覆盖 → 提交推送。
2. empro-tts-proxy.js 重新粘贴 Worker → Deploy（本版必须）。
3. 刷两次到 emp-v119，点 NLM 胶囊 Refresh。
