# EMPro v123 — 息屏播中文释义（含 v113—v122 全部内容，替代此前各包）

导出、生成器（CJK 单音色）、后台队列与磁带全链已带中文释义，
按 autoplay_cn 偏好生效；前台保持设备原生中文音（neural 中文
有口音，闭眼复习可接受、首选体验不行）。

生效步骤（缺一不可）：
1. 解压覆盖 → 推送 → 刷两次到 emp-v123。
2. App 里重新导出 wordlist 提交（释义随之进词表，触发构建；
   约新增 561 条单音色中文剪辑，体量小）。
3. 构建完成后手机重新下载音频包（只拉变化的分包）。
4. 开连播 → 息屏：词、英文释义、中文释义、搭配、例句全链出声。
构建完成前旧词的中文在息屏下会跳过，属预期。
