# EMPro v112 — 设置页可调页签顺序

设置 → Study 新增 Tab order 列表，每个页签用 ▲▼ 上下移，
顺序即时生效并持久保存（tab_order 偏好，随 Gist 同步）。
排在第一位的页签为启动默认页。未来新增页签自动排在末尾。

操作：解压覆盖 → 提交推送 → 手机刷两次到 emp-v112。
Worker 无改动。注意本包与 v111 都含 index.html/sw.js，
若尚未应用 v111，请先应用 v111 再覆盖本包。
